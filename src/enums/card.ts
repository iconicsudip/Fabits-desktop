export enum CardType {
    Amount = 'amount',
    Date = 'date',
    Health = 'health'
}

export enum HealthType {
    Poor = 'Poor',
    Weak = 'Weak',
    Good = 'Good',
    Excellent = 'Excellent'
}